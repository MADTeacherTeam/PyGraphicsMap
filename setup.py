from setuptools import setup

setup(name='PyGraphicsMap',
      version='0.1',
      description='PyGraphicsMap library can be used for software development in Python programming language, where work with dynamic objects on the cartographic basis is required and no need to use special GIS like ArchGIS or QGIS',
      packages=['PyGraphicsMap'],
      author_email='ger-duk@bk.ru',
      zip_safe=False)